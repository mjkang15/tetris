services:
  backend:
    build: ./backend
    environment:
      DATABASE_URL: sqlite:///./tetris.db
      SECRET_KEY: tetris-secret-key-change-in-production-2026
      CORS_ORIGINS: "*"
    ports:
      - "8000:8000"
    volumes:
      - ./backend/tetris.db:/app/tetris.db
